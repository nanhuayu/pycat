"""Mode system — single source of truth for mode definitions.

Public API:
- ``core.modes.types``       – ModeConfig, ToolCategoryOptions, normalize_mode_slug
- ``core.modes.defaults``    – built-in mode list
- ``core.modes.manager``     – ModeManager, resolve_mode_config
"""
